// Package myproject is the core implementation of the myproject application.
//
// The cmd/myproject binary is a thin wrapper that delegates to Run. Keep
// behavior worth testing in this package — the binary should stay small
// enough that an integration test can replace it with a single function call.
package myproject
