# Project-built binaries on PATH (after `make build`)
PATH_add "$PWD/bin"

# Go module cache + build cache stay user-global; no per-project env needed.

# Load secrets from .env if present (gitignored)
dotenv_if_exists
